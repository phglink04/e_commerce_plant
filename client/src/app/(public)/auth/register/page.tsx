import RegisterForm from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Create Account - PlantWorld",
  description: "Create a new PlantWorld account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
